[project]
name = "theta-spec"
version = "0.1.0"
description = "A declarative, harness-agnostic configuration standard for AI coding agents."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mkdocs-material>=9.7.6",
]

[tool.setuptools]
packages = []
